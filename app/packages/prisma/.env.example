# Read by the Prisma CLI (generate / migrate dev / migrate deploy), which
# resolves .env relative to this schema.prisma's own directory when run via
# `npm run --workspace=packages/prisma`. Keep in sync with ../../api/.env
# and ../../ai/.env.
DATABASE_URL="postgresql://postgres:password@localhost:5432/datacon"
